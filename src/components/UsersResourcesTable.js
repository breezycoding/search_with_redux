import React from "react";

export const UsersResourcesTable = (props) => {
    console.log(props);
    return(
        <div>
            <fieldset>
                <legend></legend>
            </fieldset>
        </div>
    );
}